print(
    "Circular imports occur when two or more modules depend on each other directly or indirectly, creating a dependency loop.\nShifting imports involves restructuring your code to import modules only when they are needed, typically inside functions or methods rather than at the module level."
)
print("\n")
print(
    "lazy_loading:\nInstead of importing a module at the beginning of another module (which triggers immediate execution of the imported module), you defer the import until it is actually needed, typically inside a function or method."
)


