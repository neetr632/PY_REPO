print("what are packages?")
print(
    """ 
    packages are a collection of modules.
    a directory with a file named "__init__.py"
    that lets the python interpreter treat the directory as a package rather than a normal directory.  
    """
)
print("explain the __init__.py file?")
print(
    """the file named "__init__.py" is used to initialize the Python interpreter with information related to the order of the modules that needs to be imported."""
)

print("__all__ is used to import all the modules inside the ___init__.py file")
print("from modulename import modulename* is used to import all the modules ")